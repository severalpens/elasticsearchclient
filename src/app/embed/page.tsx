import Link from "next/link";


export default async function Page() {
  const apiUrl = process.env.NEXT_PUBLIC_API_URL || 'https://esresearch.azurewebsites.net';
  const indexName = 'faqs';
  const embeddingType = 'standard';
  return (
    <div>
    </div>
  )
}